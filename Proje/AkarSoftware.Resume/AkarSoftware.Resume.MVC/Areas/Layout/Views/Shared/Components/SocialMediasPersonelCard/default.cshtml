@model List<SocialMediaListDto>

@if (Model != null)
{
		<div class="social text-center">

	@foreach (var item in Model)
	{
		<!-- Social -->
			<a class="social__link" href="@item.SocialMediaUrl"><i class="@item.IconName"></i></a>
		
	}
	</div>

}

